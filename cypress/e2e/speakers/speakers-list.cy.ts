describe("Speakers Page", () => {
  it("should load speakers list and speaker details", () => {
    // This is a placeholder for the speakers page test
    cy.visit("/");
    // cy.contains('Speakers').click();
    // cy.get('.speaker-card').first().click();
  });
});
